# 📊 React-AI 점수 체계 변천사

이 문서는 `react-ai`의 위험도 점수 산출 로직과 임계값(Threshold)의 변화 과정을 기록합니다.

---

## 📅 2026-02-06 (현재 버전)

**목표**: 오탐(False Positive)을 줄이고, Miss Rate(미탐)를 개선하기 위한 정밀 조정.

### 주요 변경 사항
1.  **BLOCK 임계값 하향 (60 → 55)**:
    *   AI가 0.8~0.9 등급을 줘도 다른 요소가 합쳐졌을 때 확실하게 차단하기 위함.
2.  **WARNING 임계값 상향 (30 → 35)**:
    *   단순히 정보가 부족하거나(검색 결과 없음) 신규 도메인인 경우 과도하게 WARNING을 띄우는 것 방지.
3.  **검색 불일치 페널티 완화 (30 → 15)**:
    *   `MISMATCH_SCORE`를 30점에서 15점으로 대폭 하향.
    *   검색 결과가 없는 합법적인 신규 사이트들이 차단되는 문제 해결.
4.  **Special Rule (위험 키워드) 제거**:
    *   다국어 환경 및 문맥 파악 문제로 인해 키워드 기반 하드코딩 룰(`DANGEROUS_KEYWORDS`)은 AI 판단에 맡기기로 하고 제거함.
5.  **PhishTank 데이터셋 도입**:
    *   테스트 검증을 위해 50,000개 이상의 PhishTank 데이터셋 연동.

### 현재 설정 (`config.py`)
```python
BLOCK_THRESHOLD = 55       # 차단 기준
WARNING_THRESHOLD = 35     # 경고 기준
MISMATCH_SCORE = 15        # 검색 결과 없음 페널티
TYPOSQUATTING_SCORE = 50   # 유사 도메인 발견 시 페널티
```

---

## 📅 2026-01-30 (초기 버전)

**목표**: 기본적인 AI 분석 및 검색 엔진 교차 검증 시스템 구축.

### 특징
*   초기 모델: Claude AI 점수 + 검색 엔진 검증 점수 합산 방식
*   비교적 보수적인 차단 기준 적용

### 당시 설정
```python
BLOCK_THRESHOLD = 60
WARNING_THRESHOLD = 30
MISMATCH_SCORE = 30
TYPOSQUATTING_SCORE = 50
```

---

## 📈 점수 계산 로직 (Phase별)

최종 점수 = `Phase 1` + `Phase 2` + `Phase 3` + `Phase 4`

| Phase | 항목 | 점수 | 설명 |
|-------|------|------|------|
| **1. 리다이렉트** | Hop당 점수 | +5점/회 | 리다이렉트가 많을수록 의심 |
| **2. 도메인** | 신규 도메인 | +15점 | 생성된 지 14일 이내 |
| | 수상한 TLD | +10점 | .xyz, .top 등 |
| **3. AI 분석** | AI Risk Score | 0~30점 | Claude가 판단한 위험도(0.0~1.0) × 30 |
| **4. 검색 검증** | 타이포스쿼팅 | +50점 | 유명 사이트 사칭 (google.com -> g0ogle.com) |
| | 검색 결과 없음 | **+15점** | 구글 검색 결과에 사이트 정보 없음 (기존 30) |

---

## 🧪 테스트 결과 (2026-02-06 기준)

`assess_model.py`를 사용한 300개 무작위 샘플링 테스트 결과:

*   **Detection Rate**: ~90% (유효한 악성 URL 기준)
*   **False Positive Rate**: <5% (정상 사이트 오탐)
*   **이슈**: PhishTank의 무료 데이터셋 다수가 접속 불가능(Dead) 상태라 실제 테스트 시 `FAIL` 비율이 높게 나타남. 상용 데이터셋 사용 시 정확도 상승 예상.
